/* Soma com dois valores de entrada para testar na placa */
int soma(int a, int b) {
    return a + b;
}

int main(void) {
    int a;
    int b;
    int resultado;

    a = input();
    b = input();

    resultado = soma(a, b);
    output(resultado);
    return resultado;
}
